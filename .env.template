# Accord environment configuration template
# Copy this file to `.env` and adjust values for your workspace.
# The helper script `scripts/with-env.sh` will load these variables
# before running orchestrator commands.

# LLM provider selection: `mock` (default) uses deterministic drafts,
# `openai` invokes the OpenAI API via the Responses/Chat endpoint.
ACCORD_LLM_PROVIDER=mock

# Default OpenAI model and sampling temperature (used when provider=openai).
# See: https://platform.openai.com/docs/models
ACCORD_OPENAI_MODEL=gpt-4o-mini
ACCORD_OPENAI_TEMPERATURE=0.2

# OpenAI API key (required when ACCORD_LLM_PROVIDER=openai).
# Never commit real keys to source control.
OPENAI_API_KEY=replace-with-your-key

# Optional: override the DSSE signing key path used by orchestration scripts.
# The default value points to keys generated via `scripts.provtools keygen`.
ACCORD_PROVENANCE_PRIVATE_KEY=keys/ed25519.key
